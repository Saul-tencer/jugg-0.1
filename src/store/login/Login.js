const state = {
  isloading: false
}

const getters = {}

const actions = {}

const mutations = {
  setLoading (state, val) {
    state.isloading = val
  }
}

export default{
  state,
  getters,
  actions,
  mutations
}
